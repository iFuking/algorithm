#include <iostream>
#include <cstring>
#include <algorithm>
using namespace std;

bool cmp(const char &ch1, const char &ch2) { return ch1 > ch2; }

int main()
{
	char ch = '+';
	string s = "";
	s += ch;
	return 0;
}