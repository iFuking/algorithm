#include <iostream>
using namespace std;

const int maxn = 1e5 + 10;
int num[maxn];

void solve()
{
	int N; cin >> N;
	for (int i = 0; i < N; ++i) scanf("%d", &num[i]);
	
	for (int i = 0; i < N; ++i) {
		
	}
	return;
}

int main()
{
	solve();
	return 0;
}